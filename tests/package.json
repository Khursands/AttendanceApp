{
  "name": "attendanceapp-tests",
  "version": "1.0.0",
  "private": true,
  "description": "API integration test suite (JavaScript) for the AttendanceApp PHP backend",
  "type": "commonjs",
  "scripts": {
    "test": "mocha --timeout 20000 \"specs/**/*.spec.js\"",
    "test:smoke": "mocha --timeout 20000 specs/health.spec.js"
  },
  "devDependencies": {
    "axios": "^1.7.7",
    "chai": "^4.4.1",
    "dotenv": "^16.4.5",
    "mocha": "^10.7.3"
  }
}
